### 1.3 Prefix Sum

**How to spot it:** "subarray sum", range sum query, cumulative totals, the same array queried for sums again and again — precompute O(n), query O(1)

**Demo: Subarray Sum Equals K** — [LC 560](https://leetcode.com/problems/subarray-sum-equals-k/) _(Medium)_

**Statement (Demo):** Given an integer array `nums` and an integer `k`, return the number of contiguous subarrays that sum to `k`. Negative numbers are allowed.
Example: `nums=[1,1,1], k=2` → `2` | ⚡ `n ≤ 2×10⁴`, `-1000 ≤ nums[i] ≤ 1000`

**Approach:** Keep a running prefix sum. `sum[i..j] = k` means `prefix[j] - prefix[i-1] = k`, so ask: how many earlier prefixes equal `prefix - k`? Store prefix counts in a hashmap. This works with negative numbers, where a sliding window fails.

```js
function subarraySum(nums, k) {
  const seen = new Map([[0, 1]]); // the empty prefix
  let sum = 0,
    count = 0;
  for (const x of nums) {
    sum += x;
    count += seen.get(sum - k) || 0;
    seen.set(sum, (seen.get(sum) || 0) + 1);
  }
  return count;
}
```

**Complexity:** Time O(n), Space O(n)

**Problems:**

- [ ] **Range Sum Query — Immutable** — [LC 303](https://leetcode.com/problems/range-sum-query-immutable/) — ⚪ Bonus
      → Statement: Build a class from an integer array `nums` whose `sumRange(left, right)` returns the sum of `nums[left..right]` in O(1).
      Example: `nums=[-2,0,3,-5,2,-1]`, `sumRange(0,2)` → `1` | ⚡ `n ≤ 10⁴`, queries ≤ `10⁴`
- [ ] **Count of Range Sum** — [LC 327](https://leetcode.com/problems/count-of-range-sum/) — ⚪ Bonus _(Hard — prefix sums + merge sort/BIT)_
      → Statement: Given an integer array `nums` and bounds `lower`, `upper`, count the subarrays whose sum lies in `[lower, upper]`.
      Example: `nums=[-2,5,-1], lower=-2, upper=2` → `3` | ⚡ `n ≤ 10⁵`
